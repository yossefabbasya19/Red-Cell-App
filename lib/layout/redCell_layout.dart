import 'package:flutter/material.dart';

class redCell_layout extends StatelessWidget {
  const redCell_layout({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text("data"),));
  }
}