import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class second_sign_up_screen extends StatelessWidget {
  String username;
  String email;
  String password;

   second_sign_up_screen({required this.username, required this.email, required this.password});
  Widget build(BuildContext context) {
    print(username);
    return Scaffold(appBar: AppBar() ,);
  }
}
