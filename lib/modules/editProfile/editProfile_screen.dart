import 'package:flutter/material.dart';

class editProfile_screen extends StatelessWidget {
  const editProfile_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
      child: Text("editProfile screen"),
    ));
  }
}
